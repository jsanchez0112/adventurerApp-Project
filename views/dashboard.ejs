<!DOCTYPE html>
<html lang="en">
<head>
      <meta charset="UTF-8">
      <meta http-equiv="X-UA-Compatible" content="IE=edge">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Events</title>
      <link rel="stylesheet" href="/styles.css">
</head>
<body>
      <%- include('./partials/nav.ejs') %>
      <header>Events!</header>

<% for(let i = 0; i < Events.length; i++) {%>
<table>
      <tr>
            <th>Date</th>
            <th>Location</th>
      </tr>
      <tr>
            <td><%=Events[i].date%></td>  
            <td><a href="/events/<%= Events[i]._id %>"><%= Events[i].location%></a></td>
      </tr>

</table>
<% } %>
<!-- List out the events here using a table so we can include the event happening and then the city it is happening in. -->
   
</body>
</html>